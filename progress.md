# Progress

- [x] Scaffolded project and initial test harness.
- [x] Implemented deterministic 8x8 match-3 gameplay loop.
- [x] Added timed-level twist with target gates and fail-on-timeout rules.
- [x] Added runtime input handling (swap, restart, pause, reset).
- [x] Exposed browser hooks `window.advanceTime(ms)` and `window.render_game_to_text()`.
- [x] Added deterministic scenario hooks for automation verification.
- [x] Added Playwright capture with screenshots, render snapshot, and action payload artifacts.
- [x] Verified with `pnpm install`, `pnpm test`, `pnpm build`, and `pnpm capture`.
