# Implementation Plan

1. Build deterministic match-3 engine with timed levels.
2. Add renderer + input + browser hooks.
3. Add tests and Playwright capture artifacts.
4. Run verification and automation record updates.
